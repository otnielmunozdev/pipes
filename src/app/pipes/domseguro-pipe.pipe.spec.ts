import { DomseguroPipePipe } from './domseguro-pipe.pipe';

describe('DomseguroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DomseguroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
